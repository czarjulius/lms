/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['utfs.io', 'studywebdevelopment.com', 'fresheropenings.com', 'www.codespot.org'],
  },
};

module.exports = nextConfig;
